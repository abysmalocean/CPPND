#include "route_model.h"
#include <algorithm>


RouteModel::RouteModel(const std::vector<std::byte> &xml) : Model(xml)
{
    // Create RouteModel nodes
    int counter = 0; 
    //for (Model::Node node : this->Node())
    //{
    //}

}